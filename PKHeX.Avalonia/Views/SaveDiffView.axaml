<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:PKHeX.Presentation.ViewModels;assembly=PKHeX.Presentation"
             xmlns:loc="using:PKHeX.Avalonia.Localization"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="550"
             x:Class="PKHeX.Avalonia.Views.SaveDiffView"
             x:DataType="vm:SaveDiffViewModel"
             MinWidth="600" MinHeight="400" Width="800" Height="550">

    <Border Classes="view-container">
    <DockPanel>
        <Border DockPanel.Dock="Top" Classes="tool-header">
            <StackPanel Spacing="8">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <TextBlock Text="{loc:Loc SaveDiffView_BackupA}" VerticalAlignment="Center" Width="70" />
                    <ComboBox ItemsSource="{Binding AvailableBackups}" SelectedItem="{Binding SelectedBackupA}"
                              DisplayMemberBinding="{Binding Timestamp}" Width="220" />
                    <TextBlock Text="{loc:Loc SaveDiffView_BackupB}" VerticalAlignment="Center" Width="70" />
                    <ComboBox ItemsSource="{Binding AvailableBackups}" SelectedItem="{Binding SelectedBackupB}"
                              DisplayMemberBinding="{Binding Timestamp}" Width="220" />
                    <Button Content="{loc:Loc SaveDiffView_RefreshList}" Command="{Binding RefreshBackupsCommand}" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <Button Content="{loc:Loc SaveDiffView_CompareCurrentVsA}" Classes="accent"
                            Command="{Binding CompareCurrentWithBackupCommand}" />
                    <Button Content="{loc:Loc SaveDiffView_CompareAVsB}"
                            Command="{Binding CompareTwoBackupsCommand}" />
                </StackPanel>
                <TextBlock Text="{Binding StatusText}" Foreground="{DynamicResource ThemeTextMutedBrush}"
                           FontSize="12" TextWrapping="Wrap" />
            </StackPanel>
        </Border>

        <DataGrid Classes="save-grid" ItemsSource="{Binding Changes}" CanUserSortColumns="True" IsReadOnly="True"
                  CanUserResizeColumns="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="{loc:Loc SaveDiffView_ColumnCategory}" Binding="{Binding Category}" Width="100"/>
                <DataGridTextColumn Header="{loc:Loc SaveDiffView_ColumnChange}" Binding="{Binding Description}" Width="200"/>
                <DataGridTextColumn Header="{loc:Loc SaveDiffView_ColumnBefore}" Binding="{Binding OldValue}" Width="*"/>
                <DataGridTextColumn Header="{loc:Loc SaveDiffView_ColumnAfter}" Binding="{Binding NewValue}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
    </Border>
</UserControl>
